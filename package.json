{
  "name": "finanzapp",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.js",
    "serve": "npm run build && npx -y firebase-tools emulators:start",
    "deploy": "npm run build && npx -y firebase-tools deploy",
    "test": "vitest run"
  },
  "devDependencies": {
    "dotenv": "^16.4.5",
    "entities": "^8.1.0",
    "happy-dom": "^20.14.3",
    "vite": "^8.2.2",
    "vitest": "^5.0.0"
  },
  "dependencies": {
    "lucide": "^1.39.0"
  }
}
